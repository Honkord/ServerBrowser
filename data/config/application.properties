# Server Browser — paths are relative to data/ unless noted
server.port=8443
data.dir=data

db.file=db/server_browser.db
db.schema=db/schema.sql

ssl.keystore=ssl/key.jks
ssl.keystore.password=password
ssl.alias=serverbrowser

dns.phonebook.seed=dns_phonebook/server_browser.org.json
frontend.dist=frontend/dist

# Corporate OAuth credentials live in data/config/oauth.properties (see oauth.properties.example)
